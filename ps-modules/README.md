## Localhost Listener Audit
This script enumerates all TCP listeners on localhost and maps them to their owning processes.
Output is exported to `localhost_listeners.csv`.

### Usage
```powershell
.\Get-LocalhostListeners.ps1